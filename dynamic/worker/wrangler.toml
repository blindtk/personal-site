# Cloudflare Worker das features de segurança. Ver README.md.
name = "personal-site-worker"
main = "src/index.js"
# Era 2025-01-01 (18 meses de correções de runtime e defaults por adotar,
# apanhado numa revisão de segurança). O Worker só usa fetch/Response/
# crypto/URL/Cache API/KV — nada exótico — mas uma mudança de comportamento
# por omissão não se testa por `node --test` (isso é lógica pura, sem
# runtime da Cloudflare). Antes de fazer merge desta branch: validar com o
# fluxo já documentado no CLAUDE.md (`git checkout <branch> && cd
# dynamic/worker && npx wrangler deploy`, testar a partir da branch sem
# merge) — correr `npm test` outra vez e visitar /api/health, /api/mirror,
# /api/scan a partir do deploy da branch.
compatibility_date = "2026-01-01"

# Sem subdomínio *.workers.dev nem preview URLs: o deploy final vive só nas
# rotas do domínio (routes abaixo). Um *.workers.dev público contornava a
# Access application e o WAF da zona, que só cobrem danielmala.co.
workers_dev = false
preview_urls = false

# --- Rotas no domínio ---
# Faz o Worker interceptar os paths-isco e a API no próprio site, mantendo
# a CSP connect-src 'self'. O resto do site continua servido pelo Pages.
# Tem de ficar antes do [vars] abaixo: em TOML, uma chave solta depois de
# abrir uma tabela ([vars]) pertence a essa tabela — foi assim que "routes"
# acabou a ser lido como env.routes (variável), em vez da config de rotas.
routes = [
  { pattern = "danielmala.co/api/*", zone_name = "danielmala.co" },
  { pattern = "danielmala.co/wp-login.php", zone_name = "danielmala.co" },
  { pattern = "danielmala.co/.env", zone_name = "danielmala.co" },
  { pattern = "danielmala.co/admin", zone_name = "danielmala.co" },
  { pattern = "danielmala.co/phpmyadmin/*", zone_name = "danielmala.co" },
  { pattern = "danielmala.co/.git/config", zone_name = "danielmala.co" },
]

# Um namespace KV. Cria-o e cola o id:
#   npx wrangler kv namespace create HONEYPOT
#   npx wrangler kv namespace create HONEYPOT --preview
[[kv_namespaces]]
binding = "KV"
id = "d8c992d39f464bca85135f014d3ca37b"
preview_id = "6ecf59e3f7a742748e4cc22c229543c3"

[vars]
# Origens autorizadas a chamar a API por CORS (só necessário quando o
# frontend está noutra origem, ex.: *.pages.dev a chamar *.workers.dev).
# No deploy final (Worker em rotas do próprio domínio) fica vazio — as
# chamadas são same-origin e a CSP connect-src 'self' basta.
ALLOWED_ORIGINS = ""
# Alvo do self-scan (o próprio site). O vigia CT (/api/ct) deriva daqui o
# domínio a vigiar nos logs de Certificate Transparency.
SCAN_TARGET = "https://danielmala.co/"
# Emissores esperados no vigia CT (CSV, match por substring). Vazio = a
# lista por omissão em src/lib/ct.js (Let's Encrypt + Google Trust
# Services, o par primário/backup que a Cloudflare usa). Qualquer
# certificado de outro emissor aparece como "inesperado" no painel.
CT_EXPECTED_ISSUERS = ""
# Marca temporal do deploy (epoch ms) para "tempo até 1.º scan". Opcional:
# se vazio, usa a 1.ª tentativa registada. Define no deploy, ex.:
#   npx wrangler deploy --var DEPLOY_TS:$(date +%s000)
#
# ATENÇÃO (2026-08): estando vazio, `deployTs` fica igual a `firstScanTs` e a
# métrica é zero POR CONSTRUÇÃO — não é uma medição. Era o que estava em
# produção (o painel anunciava "0s até ao 1.º scan"); honeypotStats passou a
# devolver null nesse caso e o cartão mostra "—". Para o passar a medir a
# sério não basta preencher esta var: o `meta` do KV só é escrito uma vez e
# já tem os dois timestamps iguais. É preciso apagar a chave e voltar a
# publicar com a var:
#   npx wrangler kv key delete meta --binding KV --remote
#   npx wrangler deploy --var DEPLOY_TS:$(date +%s000)
# (o primeiro scan que chegar depois disso é que passa a ser cronometrado).
DEPLOY_TS = ""
# IDs da zona/conta para o painel "Estado da Cloudflare" (/api/cf-stats,
# GraphQL Analytics API — NÃO o Radar, ver src/lib/cf-analytics.js). Não
# são segredos (identificam a zona/conta, não dão acesso sozinhos), mas sem
# eles a rota fica desativada (502 → painel mostra o fallback). IDs em
# dash.cloudflare.com → domínio → Visão geral, secção "API" no fundo da
# página lateral direita.
CF_ZONE_TAG = "b06be445835cdfec46c6084badf84d5c"
CF_ACCOUNT_ID = "336ca891b77258bead7b06fb0f9a28da"
# Nome do script deste Worker na conta. Bate com `name` no topo deste
# ficheiro — só precisa de mudar se um dos dois for renomeado.
CF_WORKER_SCRIPT = "personal-site-worker"

# Secrets (NÃO ficam aqui — o gitleaks do CI apanha qualquer deslize):
#   npx wrangler secret put RATE_SALT    # segredo p/ o hash de rate limit
#   npx wrangler secret put NVD_API_KEY  # opcional, sobe o rate limit do NVD
#   npx wrangler secret put CF_API_TOKEN # token criado em dash.cloudflare.com → Meu perfil → Tokens de API
#     · Base (pedidos/cache/ameaças/país/código HTTP, 7d): "Account
#       Analytics:Read" + "Zone Analytics:Read".
#     · Eventos de firewall por ação/origem/URL/user-agent/ASN (24h,
#       firewallEventsAdaptive CRU, dois pedidos separados): juntar as
#       permissões de leitura de firewall — "Zone Firewall Services:Read",
#       "Zone WAF:Read" e "Account Firewall Access Rules:Read". Sem elas os
#       sub-pedidos são best-effort e as tabelas de firewall ficam vazias, mas
#       o painel-base mantém-se. NOTA: o dataset AGREGADO
#       (firewallEventsAdaptive**Groups**) é Pro+ e no Free dá "does not have
#       access to the path" — por isso usamos o cru e agregamos no Worker.
#       `clientIP` está disponível no cru mas nunca é pedido (zero-PII por
#       escolha, não limitação do plano). Ver src/lib/cf-analytics.js e
#       dynamic/PLAN.md.
#
# Rotação do RATE_SALT: rodar SEMANALMENTE. Trocar o segredo invalida de
# imediato TODOS os rate-limits acumulados (o hash deixa de bater) — é
# intencional, o botão de pânico contra abuso. A rotação diária do hash é
# automática (salt = RATE_SALT + data UTC); esta é a rotação do próprio
# segredo. Ver src/lib/ratelimit.js.
#
# Rotação do CF_API_TOKEN: sem cadência automática (ao contrário do
# RATE_SALT) — é só leitura (Account/Zone Analytics:Read, mais os escopos de
# firewall acima), sem o mesmo "botão de pânico" contra abuso ativo. Rodar
# manualmente sempre que o token for exposto por engano (ex.: log, captura
# de ecrã) ou, por disciplina, ~anualmente: revogar o antigo em
# dash.cloudflare.com → Meu perfil → Tokens de API e `wrangler secret put
# CF_API_TOKEN` com o novo. Achado numa revisão de segurança (2026-07-29):
# faltava esta nota, ao contrário do RATE_SALT que já a tinha.

# --- Cron para aquecer caches e acumular janelas de 7 dias ---
# A cada 30 min: aquece ticker/scan/ct/cf-stats/threat-intel (para as visitas
# caírem sempre em cache). O snapshot da firewall (fw:<dia>, para acumular 7
# dias a partir do dataset cru do Free, que só retém 24h) só é fotografado
# quando o cf-stats É DE FACTO refrescado (TTL 6h, ~4×/dia) — não em cada um
# dos 48 ticks. TTLs de threat-intel/cf-stats/scan/ct alinhados a 6h (≥
# intervalo do cron): abaixo disso, cada tick encontrava a cache sempre
# "stale" e repetia leituras caras ao KV (2026-07, alerta real de 50% do teto
# diário do plano Free com o site ainda nem publicado — ver dynamic/PLAN.md).
# Ver scheduled() em src/index.js.
[triggers]
crons = ["*/30 * * * *"]

# NOTA (2026-07-29): a ronda 4 da revisão de segurança tinha adicionado aqui
# um bloco [limits] cpu_ms = 10 para tornar explícito o teto que o plano Free
# já impõe fisicamente. Revertido: a API da Cloudflare rejeita QUALQUER
# `limits.cpu_ms` no plano Free ("CPU limits are not supported for the Free
# plan", code 100328) — não é possível configurar isto sem upgrade de plano.
# Isto partiu todos os deploys do Worker desde esse merge (dashboards da API
# vazios em produção porque nenhum deploy seguinte chegou a publicar-se). Se
# a conta subir para um plano pago, o teto pode voltar a ser tornado
# explícito aqui.

# Observabilidade dos logs do Worker (console.error/log): sem isto, os
# `honeypot_write_failed`/`csp_report_write_failed`/`request_failed`/
# `rate_salt_missing` nunca ficam retidos em lado nenhum — não há resposta a
# incidentes possível, só reconstituição a partir de nada. head_sampling_rate
# 1 = sem amostragem (o volume de pedidos aqui é baixo o suficiente para não
# haver custo real em reter tudo). TEM de ficar no fim do ficheiro: uma
# chave solta a seguir a uma tabela pertence a essa tabela em TOML (mesmo
# aviso que já existe acima para routes/[vars]) — colocar [observability]
# mais cedo engolia workers_dev/preview_urls/routes para dentro dela.
[observability]
enabled = true
head_sampling_rate = 1
