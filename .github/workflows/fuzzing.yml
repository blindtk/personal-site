# ClusterFuzzLite (Jazzer.js) sobre as duas funções puras do Worker que
# processam bytes vindos de fora da rede sem qualquer confiança prévia:
# parseReports() (dynamic/worker/src/lib/csp-report.js) recebe o corpo bruto
# que qualquer browser de visitante pode enviar ao endpoint de relatórios
# CSP, e sanitizeText()/escapeHtml() (dynamic/worker/src/lib/sanitize.js) são
# os sanitizadores clássicos de output derivado de fontes externas. Ao
# contrário das ferramentas client-side de /ferramentas/ (sem rede, sem
# fronteira de confiança real), estas duas funções são a fronteira de
# confiança do Worker — daí serem o alvo, não as outras.
#
# Só cron semanal (não em cada PR, mesmo espírito do scorecard.yml/
# codeql.yml — ver o comentário em supply-chain.yml sobre a quota de
# minutos do plano Free do GitHub Actions) e curto (60-120s por harness): o
# objetivo é o Scorecard reconhecer a integração de fuzzing, não substituir
# os testes com vetores conhecidos já existentes (node --test).
name: Fuzzing

on:
  schedule:
    # Sábado às 7h — entre o Headers (17 6 diário) e o Invariants (43 7
    # diário), sem colidir com o Scorecard (30 5 * * 6) nem o CodeQL/Supply
    # chain (segunda de madrugada).
    - cron: '0 7 * * 6'
  workflow_dispatch: {}

permissions: {}

jobs:
  fuzz:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Build fuzzers
        id: build
        uses: google/clusterfuzzlite/actions/build_fuzzers@884713a6c30a92e5e8544c39945cd7cb630abcd1 # v1
        with:
          language: javascript

      - name: Correr fuzzers
        id: run
        uses: google/clusterfuzzlite/actions/run_fuzzers@884713a6c30a92e5e8544c39945cd7cb630abcd1 # v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          # 2 harnesses (csp_report_fuzz, sanitize_fuzz); ~120s cada,
          # dentro do orçamento total abaixo.
          fuzz-seconds: 240
          mode: 'batch'
          output-sarif: true
